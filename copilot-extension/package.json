{
  "name": "local-copilot",
  "displayName": "Local Copilot",
  "description": "A Copilot-like chat interface with local command execution capabilities. Access AI models locally via Ollama or remotely via API.",
  "version": "1.0.0",
  "publisher": "local-dev",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Chat",
    "Other"
  ],
  "activationEvents": [
    "onCommand:local-copilot.openChat",
    "onView:local-copilot.chatView"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "local-copilot-sidebar",
          "title": "Local Copilot",
          "icon": "$(comment-discussion)"
        }
      ]
    },
    "views": {
      "local-copilot-sidebar": [
        {
          "type": "webview",
          "id": "local-copilot.chatView",
          "name": "Chat"
        }
      ]
    },
    "commands": [
      {
        "command": "local-copilot.openChat",
        "title": "Local Copilot: Open Chat"
      },
      {
        "command": "local-copilot.runInTerminal",
        "title": "Local Copilot: Run Selected Command in Terminal"
      },
      {
        "command": "local-copilot.configureOllamaNetwork",
        "title": "Local Copilot: Configure Ollama for Network Access (bind 0.0.0.0)"
      },
      {
        "command": "local-copilot.toggleThinking",
        "title": "Local Copilot: Toggle Thinking Display"
      },
      {
        "command": "local-copilot.openConfigFile",
        "title": "Local Copilot: Open Config File"
      }
    ],
    "configuration": {
      "title": "Local Copilot",
      "properties": {
        "local-copilot.aiProvider": {
          "type": "string",
          "default": "ollama",
          "enum": [
            "ollama",
            "lmstudio",
            "openai",
            "copilot-web",
            "janai",
            "vscode-lm"
          ],
          "description": "AI provider to use for chat responses"
        },
        "local-copilot.ollamaEndpoint": {
          "type": "string",
          "default": "http://localhost:11434",
          "description": "Ollama API endpoint URL"
        },
        "local-copilot.ollamaHost": {
          "type": "string",
          "default": "0.0.0.0",
          "enum": ["127.0.0.1", "0.0.0.0", "localhost"],
          "description": "Ollama host binding. 0.0.0.0 allows network access, 127.0.0.1 is local only"
        },
        "local-copilot.ollamaModel": {
          "type": "string",
          "default": "qwen2.5-coder:3b",
          "description": "Ollama model name to use"
        },
        "local-copilot.lmstudioEndpoint": {
          "type": "string",
          "default": "http://127.0.0.1:1234/v1",
          "description": "LM Studio API endpoint URL"
        },
        "local-copilot.lmstudioModel": {
          "type": "string",
          "default": "",
          "description": "LM Studio model name (leave empty to use the loaded model)"
        },
        "local-copilot.openaiApiKey": {
          "type": "string",
          "default": "",
          "description": "OpenAI API key"
        },
        "local-copilot.openaiModel": {
          "type": "string",
          "default": "gpt-4o",
          "description": "OpenAI model to use"
        },
        "local-copilot.openaiEndpoint": {
          "type": "string",
          "default": "https://api.openai.com/v1",
          "description": "OpenAI-compatible API endpoint"
        },
        "local-copilot.janaiEndpoint": {
          "type": "string",
          "default": "http://127.0.0.1:1337/v1",
          "description": "JAN AI API endpoint URL"
        },
        "local-copilot.janaiModel": {
          "type": "string",
          "default": "",
          "description": "JAN AI model name (leave empty to use the loaded model)"
        },
        "local-copilot.vscodeLmModel": {
          "type": "string",
          "default": "",
          "description": "VS Code LM API model ID (leave empty to auto-select)"
        },
        "local-copilot.systemPrompt": {
          "type": "string",
          "default": "You are a local AI assistant with access to the user's terminal. You can run commands and see their output.\n\nWhen you need to run a command, wrap it in [CMD]command here[/CMD]. The command will be executed and you will receive the output automatically. You can then analyze the results and continue.\n\nRules:\n- Use [CMD]...[/CMD] to run any shell command (ls, cat, git, npm, etc.)\n- You will receive the output between [OUTPUT]...[/OUTPUT] or [ERROR]...[/ERROR] markers\n- Analyze the output and continue your work\n- You can run multiple commands in sequence\n- Be concise and helpful\n- If a command might be destructive, warn the user first\n- Never claim to be made by any specific company - you are a local model running on this machine\n- If you need to ask the user a question or clarify requirements, use [ASK]your question here[/ASK] and wait for their answer before proceeding\n- To offer the user a set of choices, use [CHOICES]option 1|option 2|option 3[/CHOICES] (pipe-separated, always include the closing [/CHOICES] tag). The user will click one and you will receive their selection. A 'Custom...' free-text option is ALWAYS added automatically so the user can type their own answer, so do NOT add a 'Custom' or 'Other' option yourself.\n- Use [READ]path/to/file[/READ] to read a file. You will receive its contents in [OUTPUT]. Supports absolute or workspace-relative paths.\n- Use [WRITE]path/to/file\ncontent here[/WRITE] to create or overwrite a file. The first line is the file path, the rest is the content.\n- Use [SEARCH]pattern[/SEARCH] to search file contents in the workspace for a regex pattern.\n- Use [FILES]**\/*.ts[/FILES] to find files matching a glob pattern (use ** for recursive).",
          "description": "System prompt for the AI model"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./ && copy src\\main.js out\\main.js",
    "watch": "tsc -watch -p ./",
    "postinstall": "node ./node_modules/vscode/bin/install"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^3.9.2",
    "typescript": "^5.3.3"
  }
}
